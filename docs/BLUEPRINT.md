# ADA Calendar — approved version one

## Purpose

Bryan wears four hats: Web (edits/builds), IT, Landings, and Software. Kyle, William, and clients independently assign work. This application makes the full workload visible while keeping work inside Monday–Friday, 9–5. All agreed V1 functionality ships together, implemented and tested in reviewable increments. Gmail/Slack reading, public links, and client-scoped portals are deferred.

**Deployment correction:** Railway replaces Vercel. Use native Next.js App Router, React, TypeScript, Tailwind and a Node 24 container. Supabase remains the database/auth/private storage/queue platform; Resend provides mail. Do not provision paid accounts or send real stakeholder mail from tests.

**September 7 environment decision:** Bryan will use one real Supabase project named **ADA Calendar** and one Railway application, connected to the existing public GitHub repository `tito3288/ADA-Calendar`. A separate hosted staging project/application is not required. Verify the real integrations in this environment while retaining isolated local automated tests and captured or explicitly allowlisted email until Bryan authorizes normal live delivery. Review the selected Supabase GitHub integration's automatic deployment settings before further pushes. Source visibility does not change invite-only application access; secrets and workload data never belong in Git. Recovery rehearsal may use an isolated local Supabase instance and remains a separate, unverified release check.

## Locked product decisions

- Timezone America/Indiana/Indianapolis; weekdays 9–17, lunch 12–12:30. September 8 owner preference: no automatic interruption reserve (reserveMinutes 0), making 450 minutes/day available for ordinary projects; unexpected work is added as it arises. Persisted workspace settings remain authoritative and are editable by Bryan; changing code must not silently replace saved settings or move existing bookings. A workspace configured with a 60-minute reserve still has 390 minutes/day.
- Owner Bryan; Kyle/William requesters; invited teammates may be viewers/requesters. All see the shared agency workload; transcripts/drafts are private.
- September 7 sign-in decision: use email and password with persistent, server-managed sessions. No public signup. Initial invitations let each person create their own password; forgotten passwords use recovery email. Normal sign-in does not request an email link. Passwords are handled by Supabase Auth, never saved in workload records or returned in API responses.
- Requesters supply effort. Clean-fit submissions commit automatically, notifying Bryan. A request that changes existing commitments waits for Bryan's approval. Requesters never edit/delete scheduled work, even their own. Requested priority is advisory; effective priority defaults Normal.
- September 9 IT default: new owner-created IT work defaults to Urgent in Add work and ADA. Explicit priority choices remain authoritative, including Normal, and edits retain saved priorities. New requester IT work suggests Urgent while retaining the existing advisory-priority approval rules. This default grants no reserve, displacement, protected-time or deadline override and does not reprioritize existing work.
- Explicit owner instructions authorize ordinary replanning. Urgent alone never overrides protected sessions or firm deadlines. Clarify/preview exceptions unless the owner explicitly authorizes them.
- Project spans and actual work sessions are different. Thin faded month ribbons communicate open work; thicker day segments show hours and protection. Week/day show exact sessions. Spans reserve zero capacity. Month opens first on desktop; mobile uses agenda.
- Owners can move one project's booked day by desktop drag or the keyboard/touch date control. The whole day's planned hours move atomically and may split across destination openings. Existing destination and unrelated bookings remain unchanged. Source IDs survive where possible; additional fragments receive deterministic IDs. Preview shows all before/after clock sessions, followed by confirmation; stale previews require renewed review. Undo restores the exact prior snapshot under version and history guards.
- Display dates do not restrict bookings. Only deliberately requested `dateConstraints` and firm deadlines are binding. Legacy top-level earliest dates, allowed dates and focus metadata remain stored but inert. Owners can inspect or change real date constraints under collapsed **Scheduling limits** without moving bookings; newly imposed limits must permit existing sessions.
- Distinguish targets, forecasts, firm deadlines, actual completion, and client-update checkpoints. Landings are client batches with progress/checklists; weekly reporting does not imply all pages due.
- Completion is explicit. Elapsed time never completes work or decrements effort without a report. Waiting work stays visible without executable sessions.
- September 8 ongoing-project decision: the owner may leave total and remaining effort unknown while booking explicitly dated work sessions. Only those sessions reserve time; their duration is not the project estimate and cannot predict a project finish. Unknown-total reservations must not be silently lost during replanning. Requesters still need positive effort estimates, and lunch/protected-time rules are unchanged.
- The scheduling increment is 15 minutes. There is no minimum-focus preference or category focus default. Hours may fit into separate openings on the requested day. Preserve unaffected sessions; new placement must be in the future. Exact version-checked Undo may restore an existing elapsed planned booking.
- Unexpected work can exchange still-usable reserve capacity for time earlier that day; count each minute once. Only the owner consumes reserve.
- Voice/typing share one validated command path. Multiple tasks may be entered together. Client aliases ground matching. Ambiguous dates, names, effort, and intent require clarification before dependent writes.
- Client choices in task forms, sidebar filters and the directory use one case-insensitive alphabetical display order, with natural numeric ordering. Directory rows remain stable while typing and re-sort on add/save. Sorting does not change client IDs, task selections or saved calendar data.
- September 8 selected-date decision: Month view has an explicit Select dates mode. One click selects a day; the second selects an inclusive range, including across months. Normal day navigation is unchanged outside that mode. Ask ADA receives typed date context for one instruction and retains it through private clarification replies, with Change/Clear controls. A work window fits the stated total effort within the range, never repeats it on every day. The owner may instead select a display-only project timeline; without separately stated work sessions it remains waiting and reserves no hours. Requesters retain positive-estimate, clean-fit-only rules. Selection alone changes no work or mail, supplies no override permission, and conflicts with spoken dates require clarification. Terminal replies, explicit cancellation, and a new instruction clear the context; errors and closing the dialog retain the unfinished draft. No provider/model, scheduler permissions, or database schema change is required.
- “I have to tell her about the completed landings” must not mark complete, claim client contact, or send mail. Offer a draft with Send/Edit/Dismiss. Clear saved work changes notify automatically without another confirmation.
- Kyle and William receive email for every saved addition, edit/reschedule, progress, completion, cancellation, and recorded client update. One operation can list all affected items. Every distinct operation remains notified. Friday 15:00 weekly summary supplements event mail.
- New external bookings and priority requests notify Bryan. Email links require login. Preview/failed transaction causes no work event/mail. Successful undo causes a corrective event; sent email cannot be recalled.
- Attach Markdown/PDF/PNG/JPEG/WebP, max five files and 20 MB per work item. Safe Markdown preview, immutable private originals, recoverable removal, external references. Audio uses separate transient private storage.

## September 9: Meetings and time off

- The owner can use **Add meeting** beside Add work to reserve fixed client meetings by title, date and start/end time. Include the client in the title. Time off uses the same flow, with an optional end date. Times use the saved workspace timezone.
- Meetings and time off are existing unavailable blocks, independent of projects and effort estimates. Their overlap with configured work hours reduces capacity; overlapping lunch/reserve/blocks count once. Saved settings stay authoritative. New tasks and Add hours schedule around these blocks.
- Fixed blocks are visible in month, week, day and agenda, including when work/client filters are active. Every actor can inspect them; only the owner can create, edit or remove them.
- Every manual addition, edit and removal is previewed before explicit confirmation through the existing scheduler and authorized, version/fingerprint-checked transaction. Preview shows the exact fixed time, any moved work before/after, and resulting availability. Protected displacement needs a fresh explicit owner override. Started/historical work cannot be displaced. Overlapping unknown-total or partially booked projects require an explicit session move first, preserving their booked hours. No credentials, external calendar integration, meeting invitations or database migration are required.

## September 9: Simple days and hours

- New work offers **Total hours**, **Days and hours**, and owner-only **Ongoing**. Total hours fits the requested amount within the selected day or range. Days and hours accepts unequal amounts on nonconsecutive dates and calculates the total. Calendar selection supplies dates; otherwise the form starts on the next usable working day. Hours require user entry.
- Ongoing projects default to unknown effort, an open-ended visible span and no reservations. **Visible through** and initial booked days are optional. Show **Hours added as needed**, without estimate warnings. Ending or cancelling the project closes an open ribbon; time passing never completes it.
- Existing work exposes **Edit details**, **Add hours**, and **Edit hours**. The day editor groups all planned clock sessions on a date into one editable hours row, including missed bookings whose time has passed; completed/cancelled sessions remain read-only. **Advanced → Set exact times** retains exact clock control. The clock never claims that a planned session was actually started.
- Every manual edit uses **Review changes → Confirm changes**, disclosing requested days, hours, actual clock slots, current total changes and Waiting → Planned where applicable. Cancelling makes no changes. Clear authorized Ask ADA creation keeps its existing confirmation behavior; the workspace helper still requires review.
- Owner-only `set_day_hours { itemId, days: [{ date, minutes }], overrideProtected? }` sets desired planned booked minutes on each named date. Dates must be unique, each amount is 0–480 in 15-minute increments, batches have at most 366 rows and 100,000 minutes. Zero removes planned bookings on that date, including missed work. Past dates may be reduced or removed but cannot receive newly added hours. The entire batch succeeds or fails together; other dates and projects remain untouched.
- Increases preserve existing sessions and find room for the difference. Decreases trim or remove the latest editable sessions first. Known remaining effort changes by the net booking delta, preserving any previously unbooked balance and original estimate. Unknown totals remain null. Removing hours neither reports completion nor refills freed hours elsewhere. Adding hours to waiting work resumes it in the same disclosed operation; removal alone does not.
- Selected booking dates, ranges and daily allocations are placement instructions for that operation. They never become permanent scheduling restrictions. A requested day's hours must fit completely on that day, around working hours, lunch, meetings, protected time and the saved interruption reserve. No spill or unrelated displacement occurs in Add hours, Edit hours or calendar moves.
- `timelineMode: "bookings"` derives the ribbon from first through last planned or completed booking. Completed dates are muted history and reserve no capacity; with no sessions, regular tasks remain in All work. `timelineMode: "span"` uses display dates, with null end meaning open-ended. Actual reservations always appear, even outside a chosen span. One shared timeline projection powers calendar and details.
- Legacy records without a timeline mode preserve unknown-estimate projects and unbooked spans as span-based work; other records follow bookings. Persist the effective mode before estimate/status changes so estimating an ongoing project cannot shorten its ribbon.
- Ask ADA extracts total hours, uniform daily hours and unequal dated hours separately, grounding every date/amount pair in the instruction. Existing-task day totals, including multiple dates and zero-hour removal, use `set_day_hours`. Booking dates are separate from ongoing display end. Only explicit lasting instructions such as “cannot start before Monday” or “only these work dates” create nested constraints. Priority defaults, clarification, authorization and requester approval remain intact.
- Forward migration `202609090003_simple_day_hours.sql` updates core transaction, daily-hours and compatibility validation. It preserves bookings, event snapshots, deadlines, protection, settings and pending/private instruction data. The cutover increments each workspace version once, invalidating old previews and pre-upgrade Undo. Pending requests are freshly planned from their proposed dated hours, with old locks used only to recover original booking intent when no proposed bookings exist.
- Release the SQL compatibility migration first and verify its applied version before deploying the application. Use isolated fixtures, local SQL and captured mail for development verification.

## September 9: Moving unfinished bookings after hours

- Owners may explicitly move planned bookings after their scheduled start or end, including reorganizing at the end of the day. The application has no recorded session-start state; a timestamp alone cannot lock unworked hours. Recorded completion/cancellation remains history.
- Calendar moves, exact-time edits, day-hour rows and grounded ADA moves use the shared rule. Moving preserves the selected full duration, remaining effort, unknown totals, original estimate and unrelated dates. Daily quotas follow the moved hours. A past source may be shortened or removed; additions and moved destinations still require available future working time. Automatic replanning does not silently displace elapsed sessions.
- Review/confirm, protection overrides, explicit date constraints, deadlines, 9–5 weekdays, lunch, meetings and saved reserve remain authoritative. Failed fits, cancelled previews and stale proposals do not change work. Undo restores the exact prior planned bookings, including elapsed times, only while the event is still the latest unchanged version.
- Forward migration `202609090004_move_unfinished_bookings.sql` replaces clock-based history guards with recorded-status guards and permits only verified exact Undo to restore elapsed planned times. It does not rewrite items, sessions, events, pending requests or settings. Ask ADA caches use `days-hours-v2`: uncommitted proposals compiled under older upcoming-only totals require a refresh; already-committed retries remain idempotent.

## September 9: Finish one booked day

- Each booked date has an owner-only **Finish this day** action with an explicit review and **Confirm day finished**. Review shows the selected date, its clock sessions, hours and remaining effort before/after using the authoritative snapshot returned with the preview, including changes made in another tab. This action completes all still-planned sessions for that project/date, including split openings, without changing any other day or project.
- `complete_day { itemId, date }` is a dedicated atomic shared-scheduler command. It subtracts newly completed booked minutes from known remaining effort (floor zero), preserves the original estimate and previously unbooked balance, and leaves unknown effort unknown. Selected daily budgets shrink by those completed minutes; other dates remain unchanged. No completion action books replacement hours automatically.
- The selected sessions retain their original dates, times and protection/reserve metadata, with status recorded as completed. Completion is explicit, never inferred from the clock. Explicit completed reserve use remains consumed even before its old scheduled end, so unrelated ordinary bookings keep their already released capacity.
- Legacy clipped session durations retain their exact stored times. Their summed duration is rounded once to whole minutes for the effort ledger. If an old selected date's remaining quota is no longer a valid 15-minute amount, remove that obsolete dated quota while retaining its residual in overall remaining effort as unbooked work. Other days and historical records remain exact.
- Details group planned and completed hours by date. A finished day shows a checkmark and done hours; remaining days stay editable. Month, week, day and agenda keep completed sessions muted and read-only. Mixed days show done and planned amounts separately; calendar movement selects only planned bookings. Completed history consumes zero planned capacity and cancelled sessions remain hidden.
- Finishing even the final booked day does not mark the project complete. **Mark project complete** remains a separate explicit action. Completion is reviewed before committing. Exact latest-event Undo may reverse this event's own planned-to-completed transition and restore its prior effort; all pre-existing completed/cancelled history stays unchanged.
- Forward migration `202609090005_complete_booked_day.sql` validates the exact selected-day completion, effort change, immutable details and unchanged neighboring work at the database boundary. It permits explicit owner completion of protected booked hours and preserves reserve accounting. The migration rewrites no live work or saved settings; deploy database compatibility first.
- Forward migration `202609090006_undo_booked_day_completion.sql` permits only exact latest-event Undo of that completion's own status transitions. It checks the authoritative stored command, full before/after snapshots and unchanged current work before restoring the original planned sessions. Other recorded history remains immutable. Only the narrow completed-day identity is exposed to the UI, derived from saved commands rather than event prose.

## September 9: ADA workspace helper

- A separate floating **ADA** button opens a keyboard-accessible corner chat on desktop and a screen-fitting panel on mobile. It remains available across workspace pages. The existing **Select dates → Ask ADA / Add work** creation flow is unchanged.
- This helper answers questions about saved work, daily agendas, weekly booking totals, visible requests and the owner's saved private notes. Notes and pending requests are not scheduled tasks and are not counted as reserved hours. Unbooked daily/weekly capacity means whole-day/week booking totals, not usable time remaining after the clock has advanced. Answers identify their saved sources and snapshot time; bounded excerpts must not be presented as complete records.
- The owner can **rearrange existing sessions on one day**, shorten/increase a booking, add hours to an existing project on a day or within a range, move a booking to another day or clock time, and transfer part of a booking to another day. It cannot create projects, delete or complete work, or edit notes or project details. Day-hour edits update known current remaining effort by their net booking change; original estimates remain unchanged. Requesters can ask questions but cannot edit existing work; viewers continue using the calendar directly. Private notes and conversation history never enter shared AppState or another actor's context.
- The helper proposes intent, not clock authority. Names/aliases, source/destination dates and hour amounts are grounded against the current instruction and existing work. Ambiguous bookings or conflicting instructions require clarification. The shared scheduler preserves whole-booking IDs, other dates and unselected sessions, avoids lunch/meetings, and honors availability, explicit date constraints, deadlines and protected-time rules. Completed/cancelled history stays unchanged. Explicit moves and day edits may select elapsed planned bookings; automatic same-day reordering still uses future openings. A failed fit changes nothing; it does not displace unrelated work or turn display spans into booking restrictions. Protected edits require fresh explicit owner permission.
- Unknown project totals remain unknown as hours are added or removed. Setting a known project's daily hours adjusts current remaining effort by the net booking change and preserves the original estimate and any unbooked balance. A direct booking request may resume waiting work, with the status change disclosed in preview. Asking to keep it waiting does not authorize executable sessions.
- Every change requires a before/after preview and an explicit **Confirm new order** or **Confirm schedule changes** click. Booking previews show dates, clock times, durations, hours freed/added, resulting daily availability and any status or total changes. Questions, speech transcription, closing the chat, selecting a day and discarding a preview never mutate the calendar. Confirmation accepts only the actor-private cached command, rechecks current authorization/version/clock/fingerprint, and uses the normal atomic/idempotent commit and notification path. Stale previews require renewed review; no automatic retry with changed times.
- Reorder requests accept natural ordering language and numbered project lists. A uniquely matched project represents all its unstarted bookings on the chosen day, preserving individual IDs, lengths and internal chronological order; multiple projects for the same client still require disambiguation. A bounded private pending reorder stores the original user request, day and requested order separately from conversational answers. Side questions never become mutation evidence or retarget that day. A short answer to a pending reorder clarification, or an explicit request to preview the remembered changes, produces a fresh preview only. Cancellation, a superseding action, a completed/no-op preview, and history expiry clear that pending request. Protected overrides must still be explicit in the latest message. No database migration or model change is required.
- The owner sees a persistent **Undo last change** card for their latest saved helper change, including after closing/reopening the chat or refreshing. This deterministic action uses the existing authorized, version-aware undo transaction, not an AI command. It restores the previous bookings and project details only while that exact event is still the latest unchanged schedule version. Undo cannot remove or alter older completed/cancelled work; a day-completion event may reverse only its own exact completion as specified above. Elapsed planned bookings remain eligible: the exact latest-event snapshot may restore their original time, while new scheduling still requires future placement. Shared server checks and the database transaction enforce these rules. Private notes remain untouched. Pending previews and unsent drafts block undo rather than being discarded. An uncertain response is reconciled by reading the saved event before any retry, never by targeting a newer change. Undo is a single eligible rollback, not unlimited history; it creates the normal corrective event/notification and cannot recall sent email.
- Chat uses a separate server endpoint and namespaced bounded private history, without consuming or changing a pending task-creation clarification. Saved documents, request text, old assistant answers and notes are reference material, not permission to act. The model/provider and usage limits remain unchanged. Local tests disable live AI and email. Legacy session focus fields may remain in JSON for compatibility, but they neither restrict scheduling nor require an owner override; authorization and protected-time checks remain enforced.

## Architecture contract

`src/lib/types.ts` defines WorkCommand, ScheduleSnapshot, ScheduleProposal, WorkEvent and AppState. Pure TypeScript scheduling is independent of providers. Model tools propose domain commands, never raw database/email operations. All mutation paths revalidate authorization, schedule version and constraints.

Persist committed changes, immutable event and per-recipient notification jobs in one database transaction. Prevent overlap and concurrent stale commits. Retry using stable operation/event IDs. Recompute stale previews; never turn a stale clean-fit request into an authorized displacement. Undo is a version-aware compensating change.

Supabase Queues and Cron run bounded mail workers independently of the browser. Track provider IDs, retries, failures and uncertain outcomes. Resend idempotency lasts 24h; uncertain older sends require reconciliation. Verify webhook signatures and handle repeated/out-of-order callbacks. Keep production credentials server-side.

OpenAI Responses model `gpt-5.6-sol`, medium reasoning, strict schema; `gpt-transcribe` recorded speech. Warn at $20 monthly usage and pause new AI calls at $25 until owner raises allowance. Manual operations remain available. Approximate total operating target $100; Railway is usage-priced, so report observed costs, not a guaranteed fixed total.

## Verification gates

1. Foundation, versions and typed rules. 2. Auth/data permissions and transactional schema. 3. Scheduler constraints, reserve, concurrency and undo. 4. Owner month/week/detail/attachments UI. 5. Requester/viewer booking and approvals. 6. Voice/text intent safety and drafts. 7. Durable emails and weekly summary. 8. Full workflows, accessibility, production build, Railway packaging and setup/restore runbook.

Mandatory examples: Drive and Shine September ribbon with protected September 9/11 sessions; six landing clients crossing week boundaries; no capacity from span alone; blocked client dependency; interruption and past reserve; explicit protection override; concurrent same-slot booking; clean fit vs displacement; future-tense no-op; retries/webhooks/browser closure; undo after later booking; timezone/DST; keyboard/touch/narrow layout.

Real account addresses, complete client list, Supabase/Railway credentials and a verified sender domain are setup inputs. Local sample data and captured mail must remain visibly identified. Never claim live integrations, delivery, deployment, or real-model evaluations were verified without evidence.
