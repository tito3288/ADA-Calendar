# Verification and release gates

This records evidence for the eight phases in [the approved blueprint](BLUEPRINT.md), not a claim that hosted services have been deployed. Sample clients, the demo parser, and captured notifications are deliberately isolated from live work.

## Alphabetical client lists — September 10, 2026

- Task create/edit dropdowns, sidebar filters and the client directory share alphabetical display ordering, ignoring case and sorting numbers naturally. Directory name edits keep their row position until add/save; original stored client order and selected IDs remain intact.
- All 1,289 unit/server tests and 11 existing directory/alias-editing browser scenarios pass, along with lint, TypeScript checking and the production build. Desktop/mobile browser inspection confirmed identical sorted names across all three lists, selection by client ID, intact name typing and readable layouts using isolated numeric/mixed-case fixtures. No production calendar mutation or real email was used for verification.

## Finish one booked day — September 9, 2026

- Added visible per-day completion with review/confirmation, grouped day history and muted completed calendar segments. The three-day example finishes only the first hour, changes remaining effort from 3h to 2h, and leaves tomorrow and the following day active. Unknown totals, original estimates and unfinished project status are preserved. Month/week/day/agenda retain done hours without reserving capacity.
- All **1,289 unit/server tests in 62 files** pass. New coverage includes split/protected hours, explicit completion before/during/after clock time, old clipped durations, partial daily budgets, unknown totals, final-day/project distinction, reserve preservation, role restrictions, replay, stale previews, unchanged neighbors and cancelled/history handling. Exact latest-event Undo restores this completion's own planned sessions, remaining effort and daily budgets while preserving older history. Server-render tests verify separate done/planned amounts and planned-only movement on mixed days.
- `scripts/day-completion-smoke.ts` passed against isolated local Supabase Auth and PostgreSQL, including direct-RPC tampering rejection, split/protected completion, 3h→2h, retained next days, unknown totals, final-day status, reserve accounting, clipped legacy work and idempotent retries. It also verifies exact Undo/replay of split/protected and past clipped completion, with forged source/history and nonowner rejection. The existing `scripts/simple-day-hours-smoke.ts` also passed, preserving day edits, missed-booking moves, protected overrides, deadlines and Undo behavior. Fixtures are removed after verification; no mail was queued or sent.
- Lint, generated-route TypeScript checking, `npm run build -- --webpack`, and local PostgreSQL schema lint pass. Browser verification passed **34 distinct scenarios**: five new desktop/mobile completion cases and 29 affected calendar-move, meeting, session-management and timed-calendar regressions. Completion review uses the current server snapshot; a regression changes the selected day from 1h to 2h in another tab before opening review and confirms that current hours/times and 4h→2h remaining are shown before any write. Final desktop/mobile screenshots were inspected for done/planned labels and overflow. These use isolated fixtures and captured providers; no production calendar work was edited.
- Database compatibility commit `01cdd26` was pushed first. Supabase confirmed migration `202609090005` and its deployment check passed. Read-only before/after hashes match for work items, all 21 sessions, blocks, historical events, pending requests and saved settings; the workspace version remains 60 and interruption reserve remains zero. The migration changes validation only. The application release follows this verified database deployment.
- Scoped Undo compatibility commit `7897ee6` followed before the application. Supabase confirmed migration `202609090006`; a second read-only hash comparison again verified unchanged work, history, requests and settings, with version 60, 21 sessions and zero reserve. Local Auth/SQL completion and existing day-hours compatibility smoke scripts both pass under the final migration, and PostgreSQL lint reports no errors.

## Moving unfinished bookings after hours — September 9, 2026

- Planned bookings remain movable after their scheduled start/end. Calendar day moves, exact-time moves, day-hour editing and grounded ADA moves preserve the selected full hours, known remaining effort, unknown totals and other days. Legacy overbooking balances do not cause a new estimate alert. Completed/cancelled history remains unchanged; new placement still respects future openings, lunch, weekdays, saved reserve, meetings, protection and genuine limits.
- All **1,234 unit/server tests in 57 files**, lint, generated-route TypeScript checks and the optimized production build pass. Old uncommitted Ask ADA caches require `days-hours-v2` refresh; already-committed retries stay idempotent. Old chat day totals that reinterpret elapsed bookings fail the current review fingerprint with no mutation.
- Desktop/mobile browser verification passed **26/26**, including five new scenarios at 6:38 PM: month moves, editing a booked day's date, exact-time moves, preview cancellation, unchanged 2h totals and retained completed/cancelled history. Screenshots were inspected for layout. Fixtures and captured providers were isolated on port 3100.
- Both actual local database smoke scripts passed. Coverage includes missed planned moves and day edits, daily quota transfer, exact Undo restoring an elapsed planned booking, duplicate Undo, forged Undo rejection, requester/viewer authorization, stale concurrent writes, protected overrides, immutable completed history, and captured notification/provider boundaries. PostgreSQL schema lint reports no errors. No external email was sent.
- Database compatibility commit `6d0dfd8` is released before the application. Migration `202609090004` changes validation functions only; it does not rewrite bookings, project data, events or workspace settings. Supabase confirmed migration `202609090004`; read-only before/after hashes verified unchanged items, sessions, blocks, event snapshots, pending requests and settings, with workspace version still 59 and reserve still zero. The application's version-checked preview and Undo rules remain authoritative.
- Application commit `adcd8ce` deployed successfully on Railway and both full GitHub CI jobs passed, including isolated browser workflows.

## Simple days and hours — September 9, 2026

- Manual creation and editing now use total hours, dated hour rows and open-ended ongoing projects. Shared scheduler coverage verifies 4h→3h edits, increases beyond the old current total, unknown totals, zero removal, split destination openings, stable IDs, explicit constraints, legacy flexibility, partial balances, started work, protection, lunch, meetings, reserve settings and atomic failures. Pending approvals retain transient booking windows; old private command caches require a fresh proposal while committed retries remain idempotent.
- All **1,189 unit/server tests in 55 files**, lint, generated-route TypeScript checks and the optimized Next.js webpack production build pass. Browser verification uses fictional fixtures and captured providers, including desktop/mobile day editors, ongoing projects, calendar moves, approvals, cancellation, stale previews, retries and Undo. All **163 distinct browser scenarios** passed across the full run and targeted reruns; ten initial failures were corrected fixture assumptions, stale labels or assertions, and a shared-fixture collision. The complete affected five-file rerun passed **46/46**. Desktop/mobile screenshots were inspected for layout and overflow. A final **10/10** session-editor rerun also verifies exact clock placement when changing both time and duration, and atomic first-estimate/page-progress reconciliation for ongoing work.
- `scripts/simple-day-hours-smoke.ts` passed against actual isolated local Supabase Auth and PostgreSQL. It checks known/unknown hours, zero removal, split moves, requesters, replay, stale writes, genuine dates/deadlines, protection, direct-RPC started/history tampering rejection and Undo. Fixtures are removed afterward; no email is queued or sent. Local PostgreSQL lint reports no schema errors.
- Database compatibility commit `d5dea0d` was pushed first. Supabase confirmed migration `202609090003`, and authenticated read-only checks verified unchanged hashes for work items, bookings, blocks, event snapshots, pending requests and saved settings. The live workspace version advanced exactly once, from 56 to 57. No production task or session was edited during verification. Railway also reported successful deployment of this compatibility commit.
- Application changes are released only after the migration is verified. The application commit's GitHub checks and Railway deployment status are the final hosted release evidence; local fixture results do not claim real-provider evaluation or live email delivery.
- Application commit `8f8e5e5` deployed successfully on Railway: health and sign-in return 200, unauthenticated calendar access returns 401. GitHub lint/types/unit/build checks passed; its browser job passed **163/163** in 14.5 minutes before the optional HTML reporter exhausted Node's heap. CI now uses Playwright's list reporter and retains raw failure traces/screenshots; local HTML reporting is unchanged. The exact CI reporter command passed a local 10/10 browser smoke. Reporting-only follow-up `9a6b417` subsequently passed both complete GitHub jobs, including all 163 browser cases, and deployed successfully on Railway.

## Allowed work dates and blocked moves — September 9, 2026

- Work details and session management expose the saved allowed dates separately from the faded span. Edit work lets the owner add/remove dates or explicitly remove the restriction; preview discloses before/after permissions. Changed dates preserve exact active-project sessions, including partial/unknown effort, and do not resume waiting work. Unchanged date permissions are omitted from updates. Month move alerts identify the actual earliest-start, deadline or allowed-date constraint.
- Lint, generated-route TypeScript checks and **1,115 unit/server tests** passed. **31 distinct browser scenarios** passed across the focused date-editor run and existing calendar-move/session-management/IT-priority regressions. Desktop/mobile tests reproduce the rejected move, add the destination without moving current bookings, then confirm a separate move. Coverage includes cancel, display-span-only edits, explicit restriction removal, waiting work, requester read-only access, protected time, unknown/partial estimates, saved quotas and insufficient destination capacity. Desktop/mobile screenshots were inspected.
- The final source passed `npm run build -- --webpack` on Node 22.22.2. Browser fixtures were isolated and no live calendar mutation or real mail send occurred. Railway deployment remains separate from this local verification.

## IT priority defaults — September 9, 2026

- New owner IT work defaults to Urgent in the manual form and ADA. Explicit choices override the default, category changes preserve a manually selected priority, and editing existing work preserves its saved priority. Requester IT suggestions default to Urgent while effective priority remains Normal under the existing approval rules. The category default grants no scheduling override.
- Lint, generated-route TypeScript checks, **1,105 unit/server tests**, and **24 API/browser scenarios** passed. Coverage includes owner desktop/mobile save/reopen of Urgent and explicit Normal, requester clean fits, existing-work category edits, model-inferred priorities, and explicit non-urgent instructions. Desktop/mobile priority screenshots were inspected. Tests use fictional workspaces and captured mail; no live work or real sends were changed.
- The final source passed `npm run build -- --webpack` on Node 22.22.2. This is local verification; Railway deployment is separate.

## Waiting project bookings — September 9, 2026

- All session-manager entry points now start with the same visible booking authorization. Switching from exact-session editing to smart fit no longer accidentally requests hours without resuming waiting work. The owner can still uncheck that choice. The preview discloses Waiting → Planned and clearing the waiting reason; unknown effort stays unknown. Smart-fit totals reflect the chosen hours rather than unused exact-session drafts.
- Lint, generated-route TypeScript checks, **1,072 unit/server tests**, and **20 focused Playwright scenarios** passed. Desktop/mobile regressions cover three hours on Friday September 11 around four existing booked hours, both exact-entry buttons, cancellation, explicit opt-out across mode switches, preserved unknown effort/project span/settings/other bookings, and preview-before-confirmation. Dialog accessibility scans passed. Fixtures and captured messages remain isolated from live work.
- `npm run build -- --webpack` passed on Node 22.22.2. The default Turbopack build encountered the local CSS-worker port-binding `EPERM`, including an escalated retry. No Railway deployment or real calendar booking was performed by this verification.

## Password authentication — September 7, 2026

- Bryan requested email/password sign-in before Resend onboarding. Normal sign-in no longer sends a magic link; access remains invite-only. Initial setup and recovery use explicitly verified, active-member sessions. Account creation and membership are completed before invitation delivery.
- Full lint and generated-route TypeScript checks pass. **194 unit/server tests in 11 files pass**, including password byte preservation and limits, active-member checks, safe errors, malformed-JSON password nonreflection, callback allowlists, CSRF denial, real-session requirements despite a demo actor, invitation ordering, no-send failure paths and auth-email isolation. No real providers are called by this suite.
- **26/26 Playwright scenarios pass**, including password-manager field semantics, visibility toggles, invalid/successful mocked sign-in, generic recovery messages, safe fixed notices, desktop/mobile accessibility and existing workload regressions. Provider requests are intercepted/credentials blanked in this browser suite.
- [Real local password smoke](../scripts/password-auth-smoke.ts) passed against the ADA-only Supabase stack and captured SMTP: pre-created unconfirmed invitation, token-hash callback, actual browser password creation/mismatch handling and email/password login, persistent HttpOnly session access, captured recovery, old-password rejection, new-password success, replay rejection, revoked-member denial and logout. Setup desktop/mobile axe scans returned zero violations; screenshots were inspected with no clipping/overflow. The test transfers the configured local template callback from port 3000 to its isolated port 3201; it does not modify the user-owned server or demonstrate the hosted template installation.
- The local Auth server empirically rejects passwords over 72 UTF-8 bytes; UI/server creation validation handles that before provider calls. New passwords require at least 12 characters. Browser tests caught Next.js global headers overriding callback Referrer-Policy; explicit auth-path overrides now verify `no-referrer` on real HTTP responses.
- The final application builds using Railway's Node **24.20.0** production Dockerfile. Image `ada-calendar:v1-local` digest: `sha256:e002500173d8aba9f99b1f1ffd0a958ac91f3bd715896b97e3f1bfb067337223`. Network-isolated container checks verify unconfigured fail-closed behavior, runtime-configured password form, non-root operation, protected state denial, assets and ffprobe. No hosted account passwords, invitations or Resend sends were changed.
- Hosted state from the setup session: seven migrations are visible in the real Supabase project and the Railway app serves its login page. Bryan has authorized committing/pushing this password change; the resulting hosted password deployment must be verified separately. Hosted SMTP, email templates, actual owner creation/password, live login/recovery, real AI and workload delivery remain setup gates, not established by local tests.

## Clarification correction and environment decision — September 7, 2026

- Bryan chose one real Supabase project (**ADA Calendar**) and one Railway app. Separate hosted staging is no longer required. Automated fixture tests still run locally, with no real provider calls or stakeholder mail.
- **131 unit/server tests in nine files passed**, along with full lint and generated-route TypeScript checks. Added coverage for short replies, private server context, multiple clarification turns, corrected dates, old override rejection, topic switches, cancellation without AI budget, concurrent replies, and exact retry after midnight. The Responses adapter is exercised with a mocked provider; this does not establish real-model accuracy.
- The expanded browser suite passed **20/20** scenarios, including original client/task/date preservation after “Two hours,” no replay on a fresh instruction, Start a new instruction, cancellation, and duplicate reply delivery. All notification assertions use captured messages.
- All **seven migrations** are applied to the isolated local ADA Supabase database. New parent/child operations passed actual SQL smoke for author/workspace privacy, membership revocation, concurrent single-child admission, no losing budget charge, replay hash/parent binding, and failed-child consumption. SQL lint returned no errors. No reset was performed; only ADA containers were stopped afterward and their volumes were preserved.
- The intent evaluation script passed its five existing single-message cases plus the new two-turn estimate scenario in deterministic demo mode. Live execution remains explicitly opt-in and proposal-only. The implementation retains bounded, server-written user/context data for interpretation, following [OpenAI conversation-state guidance](https://developers.openai.com/api/docs/guides/conversation-state); it does not treat prior assistant questions as user authority.
- Application correction commit `670567d` is pushed to `main`. GitHub Actions run [34135186046](https://github.com/tito3288/ADA-Calendar/actions/runs/34135186046) passed both the lint/types/unit-tests/production-build job and the isolated demo browser job. This verifies the correction in CI, not hosted Supabase, Railway, or live providers.
- Bryan saved the real Supabase project's GitHub production integration on September 7: repository `tito3288/ADA-Calendar`, working directory `.`, production branch `main`, automatic branching off. A documentation-only push is authorized to trigger initial deployment. Deployment and hosted migration results remain to be checked; the settings-save toast is not evidence that the seven migrations ran.
- Host `npm run check` passed lint/types/tests but the Next.js build encountered a local Turbopack port-binding `EPERM`, including on an escalated rerun. Production is verified using the Node 24 Dockerfile separately; do not report a successful host build.
- Final frozen application source built successfully with the production Dockerfile, including Next.js/Turbopack compilation and TypeScript. `ada-calendar:v1-local` digest: `sha256:38e29426c676b3594bf7094cdc2db585b782269dd9d00ce9da802b53928c1562`. Both repeatable container smoke modes passed on Node **24.20.0** with no external networking: non-root, health/static assets 200, private state 401, ffprobe available, and no bundled local environment or demo persistence. Temporary containers were removed; unrelated services were unchanged. Railway and actual hosted integrations remain unverified.

## Historical pre-correction evidence — September 7, 2026

- Latest observed unit/server run: **108 tests passed in seven files**. This includes real local audio-duration probing, deterministic scheduler cases, intent guards, email safety, authenticated-demo routes, preview fingerprints, cost estimates, trusted accounting wrappers, and mocked session-refresh behavior.
- Full lint, generated route types, TypeScript and all **108 tests** passed again on September 7. The optimized production build also passed inside the rebuilt Node 24 container; the previous full local `npm run check` passed September 5.
- Latest integrated [Playwright suite](../tests/e2e): **17/17 passed in 25.3 seconds** after the final transcription-cost and live-ledger changes. Desktop/mobile axe scans had zero violations on the tested screens; month/day/mobile screenshots were inspected. Day/week rendering produced no browser errors. The demo uses relative dates: this run demonstrated the protected Drive & Shine sessions on September 10/14, not the blueprint's literal September 9/11 example.
- The five-case [demo intent check](../scripts/evaluate-intents.ts) passed locally. It exercises a small deterministic parser, **not an OpenAI model**.
- The real local [database smoke run](../scripts/database-smoke.ts) passed after all six migrations: Auth/public-signup denial, RLS, transactional outbox/replay, competing stale writes, overlap exclusion, AI cache/budget privacy, private ready/pending files, pgmq acknowledgment, late webhook handling, explicit session completion/time reuse and weekly deduplication. Extended checks verified coherent atomic snapshots, preservation of more than 1,000 historical sessions, direct-requester protection/history rejection, and server-only AI accounting. SQL lint reported zero errors. The earlier actual local Edge-worker capture check also passed (401/405 restrictions; six jobs captured and acknowledged). These are isolated ADA Supabase checks, not hosted delivery.
- The rebuilt local Node **24.20.0** Docker image passed [repeatable production startup checks](../scripts/container-smoke.mjs): setup page returned 200 without configuration; `ADA_DEMO_MODE=true` and a forged demo cookie could not bypass production auth; `/api/state` returned 401; `ffprobe` was present; runtime-supplied public Supabase configuration exposed sign-in without rebuilding. Static assets served successfully; execution was non-root; local environment files and demo persistence were absent. Both test containers had external networking disabled and were removed afterward. This does not prove Railway deployment or successful hosted login.
- The new CI workflow is configured but has not yet run on GitHub. No production deployment, real-model evaluation, live stakeholder delivery, or backup restoration is asserted by these checks.
- Production dependency audit reported zero vulnerabilities on September 5; the September 7 locked container install also reported zero vulnerabilities. Local verification is complete for this checkpoint. See [the current handoff](RESUME.md) for the remaining hosted setup and release gates.

## Phase-to-evidence map

| Phase | Implementation | Automated evidence and acceptance boundary |
| --- | --- | --- |
| 1. Foundation and typed rules | [Types](../src/lib/types.ts), [schemas](../src/lib/schemas.ts), [defaults](../src/lib/defaults.ts), Node 24 [container](../Dockerfile) | Typecheck/build; scheduler tests validate settings, date arithmetic and malformed inputs. Package installation and compilation do not establish production connectivity. |
| 2. Auth and transactional data | [Auth](../src/lib/server/auth.ts), [live store](../src/lib/server/live-store.ts), [proxy](../src/proxy.ts), [SQL migrations](../supabase/migrations) | `proxy.test.ts` covers cookie refresh/privacy and disabled production demo bypass. `server.test.ts` checks role spoofing, private operation results and same-origin mutations. Database smoke uses actual local Auth sessions/RLS, direct-write denial, overlap exclusion and atomic event/outbox replay. Hosted invitations, SMTP and expired-session journeys still require setup. |
| 3. Scheduler and undo | [Scheduler](../src/lib/scheduler.ts), [time arithmetic](../src/lib/time.ts) | `scheduler.test.ts`: 390-minute ordinary days; spans reserve nothing; lunch/weekends/DST; focus minimums; no past placement; whole-batch rollback; retained unaffected sessions; reserve exchange/past reserve; per-project protection; explicit deadlines; waiting/resume; progress, session/project completion. `server.test.ts` and browser API tests exercise concurrent stale commits and version-aware undo. |
| 4. Owner calendar and files | [Calendar](../src/components/calendar.tsx), [timed views](../src/components/timed-calendar.tsx), [work details](../src/components/work-details.tsx), [uploads](../src/lib/server/uploads.ts) | Passing browser suite covers month-first desktop, manual preview/save/completion, protected keyboard moves, safe Markdown rendering and exact original bytes. Month/day/mobile screenshots confirm cross-week ribbon continuity, thick reserved segments, six landing clients, and lunch/reserve shading. Real microphone/touch/drag/resize and PDF/image journeys still need target-device verification. |
| 5. Requester/viewer workflows | [Work form](../src/components/work-form.tsx), [request review](../src/components/request-review.tsx), [API](../src/app/api/[...path]/route.ts) | Passing scheduler/browser tests distinguish clean fit from displacement and keep requested priority advisory. Server tests require a fresh owner-approved version and semantic preview fingerprint, sanitize requester provenance, and keep pending files private before sharing them on approval. Revised approvals, stale-review recovery, authority with a valid fingerprint, and concurrent same-opening requests passed the September 7 browser run. |
| 6. Voice/text and private drafts | [Assistant](../src/lib/server/assistant.ts), [input panel](../src/components/assistant-panel.tsx) | `intent.test.ts`: grounded names/dates/effort, ambiguity, batch safety, future-tense no-op/draft, viewer/requester denial, rejected fabricated override authority. Real WAV probing accepts 1 second and rejects 121 seconds/junk without a provider call. Server tests cache retries, reserve budget atomically and bind Send to the exact reviewed draft. Real transcription/model extraction accuracy, microphone permissions and device recordings are outstanding live gates. |
| 7. Durable and weekly email | [Email helpers](../src/lib/server/email.ts), [delivery SQL](../supabase/migrations/202609050003_delivery.sql), [Edge worker](../supabase/functions/notification-worker/index.ts) | `email.test.ts` verifies recipients, stable deduplication, corrective notifications, private drafts, capture default, allowlists and signed raw-body webhooks. Server/browser tests verify captured notifications only after commits. Database smoke checks pgmq acknowledgment, late/repeated callbacks and weekly deduplication; its optional local Edge mode tests worker authentication/method restrictions. Hosted Cron, real provider retries and delivery remain separate gates. |
| 8. Workflows, accessibility and operations | [Browser tests](../tests/e2e), [CI](../.github/workflows/ci.yml), [Railway config](../railway.toml), [runbook](RUNBOOK.md) | Browser checks include keyboard opening/Escape/focus restoration, shared read-only details, mobile agenda/overflow and axe WCAG A/AA scans of selected desktop/mobile screens. This is not a complete accessibility certification or all-browser/device test. Local production-container fail-closed behavior passed; hosted health/auth/storage, costs and restore rehearsal still require their own recorded outcomes. |

## Important behavior limits

- **Capacity means known sessions, not the entire backlog.** Unestimated, partly scheduled or waiting work can still require substantial future effort. Thin project spans add no reserved minutes. The “Not fully scheduled” warning must be considered alongside any open-time number; an apparently free day is not a promise that all commitments fit.
- **Effort is explicit.** “Monday through Tuesday” describes a window, not sixteen hours of work. Missing/ambiguous effort or dates must be clarified. Finish this day subtracts that day's booked hours from known remaining effort; the advanced individual-session completion control leaves reported effort unchanged. Neither action changes the original estimate or automatically completes the project. Time passing does not complete work.
- **Planning is bounded and greedy.** The deterministic allocation rules search a horizon of up to 366 days from the eligible start, constrained by deadlines, allowed dates, focus blocks and existing sessions. They preserve existing commitments where possible; they are not a global optimization solver. A reported conflict means this planner did not find an allowed fit, not a mathematical proof that no possible rearrangement exists.
- **Demo is not AI or delivery.** Typed demo examples support a limited grammar. Recorded demo transcription is unavailable rather than fabricated. Cached/mock model outputs test safety boundaries, not model quality. Captured mail is visible test data and is not sent or replayed later automatically.
- **Current CI has no cloud dependency.** It runs Node 24, locked installation, lint, typecheck, unit/server tests, build and isolated Chromium demo workflows. It does not run a hosted Supabase database, a paid model, real email, Railway deployment, or restoration. Local database/Edge tests are a distinct manual gate.

## Before real team use

1. Configure the real invited accounts, client aliases, timezone and verified sender. Apply migrations to the intended Supabase project; verify owner/requester/viewer isolation, invitation/password setup, subsequent password login and recovery on the final HTTPS origin.
2. Run live model/transcription evaluations with a deliberately small authorized budget. Extend the five-case proposal-only evaluation corpus with real phrasing, noisy recordings, ambiguous clients/dates, batches, requested priority, negation, quoted instructions and future-tense updates. Confirm latency, model availability and measured costs; no model calls have been validated by demo CI.
3. Exercise the hosted worker/Cron in capture mode, then a narrowly allowlisted test recipient. Verify sender-domain authentication, webhook signatures, retry/uncertain-send handling, Friday summary deduplication and delivery with the browser closed. Enable stakeholder delivery only after checking exact recipients and body text.
4. Verify Railway container startup/health and authenticated deep links, microphone/upload flows, mobile touch and timed-calendar moves on target browsers. Record a full final local/CI pass and inspect generated screenshots; axe scans do not replace keyboard and human review.
5. Rehearse a complete database **and Storage-object** restore into an isolated capture-only local destination, verify object checksums and role access, and reconcile provider IDs/queue state before enabling delivery. A second hosted project is not required under Bryan's updated decision. The runbook describes recovery; a successful recovery has not yet been demonstrated.

CI action/runtime configuration follows the official [GitHub checkout](https://github.com/actions/checkout), [Node setup](https://github.com/actions/setup-node), [artifact](https://github.com/actions/upload-artifact), and [Playwright CI](https://playwright.dev/docs/ci) guidance. Their configuration is not evidence that this repository's hosted workflow has run.
